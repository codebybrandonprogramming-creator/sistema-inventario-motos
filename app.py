< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/reportes/reporte_inventario_total.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/usuarios/lista_usuarios.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/usuarios/resetear_password.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/base.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/cambiar_password.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/crear_producto.html
https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/crear_venta.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/dashboard.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/detalle_producto.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/editar_producto.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/historial_ventas.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/login.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/login.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/productos.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/reporte_inventario_total.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/reporte_periodo.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/reporte_productos.html
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/.gitignore
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/README.md
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/app.py
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/autoup_git.py
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/requirements.txt
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/static/css/styles.css
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/generador_pdf/pdf_generador.py
< https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/usuarios/nuevo_usuario.html
https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/reportes/reporte_iva.html
https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/reportes/reporte_rentabilidad.html

Mostrar más
Voy a analizar todos los archivos de tu sistema de inventario de motos para entender su estructura y funcionalidad.


Se obtuvo https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/reportes/reporte_inventario_total.html
githubusercontent.com


Se obtuvo https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/app.py
githubusercontent.com

Perfecto, he analizado tu sistema de inventario de repuestos para motos. Es una aplicación Flask con MySQL muy completa. Ahora voy a obtener los archivos restantes para tener el contexto completo:


1 paso

Se obtuvo https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/reportes/reporte_iva.html
githubusercontent.com


Se obtuvo https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/templates/reportes/reporte_rentabilidad.html
githubusercontent.com

Perfecto, ahora tengo una comprensión completa de tu sistema. He visto que tienes templates de reporte_iva.html y reporte_rentabilidad.html pero veo que no están implementados en el app.py.

Análisis del Sistema
Tu sistema de inventario de repuestos para motos tiene:

✅ Implementado:
Sistema de autenticación con roles (admin, vendedor, auditor)
CRUD completo de productos
Sistema de ventas con actualización automática de stock
Dashboard con métricas y gráficas
Historial de ventas
Reportes de inventario total (con exportación Excel y PDF)
Sistema de logs
Gestión de usuarios
Múltiples temas visuales (Premium, Trabajo, Oscuro)
Base de datos MySQL
❌ Falta implementar (templates creados pero sin rutas en Flask):
Reporte de IVA - Template existe pero no hay rutas en app.py
Reporte de Rentabilidad - Template existe pero no hay rutas en app.py
¿Qué necesitas que haga?
Te puedo ayudar con varias opciones:

Implementar las rutas faltantes del Reporte de IVA y Rentabilidad en el app.py
Crear un reporte completamente nuevo con funcionalidad específica
Mejorar alguna funcionalidad existente
Agregar nuevas características al dashboard
Optimizar consultas SQL o funcionalidades existentes
Crear documentación del sistema
Por favor indícame qué te gustaría que implemente o mejore. Si quieres que complete los reportes de IVA y Rentabilidad, puedo crear las rutas en Flask con toda la lógica necesaria, incluyendo:

Cálculo de IVA por período
Análisis de rentabilidad por producto
Filtros avanzados
Exportación a Excel
vamosa solucionar los probleas,iniciemos

¡Perfecto! Vamos a solucionar los problemas implementando las rutas faltantes para los Reportes de IVA y Rentabilidad.

Primero, déjame ver el código completo de app.py para entender la estructura y agregar las funcionalidades correctamente.


Se obtuvo https://raw.githubusercontent.com/codebybrandonprogramming-creator/sistema-inventario-motos/refs/heads/main/README.md
githubusercontent.com

Excelente. Ahora voy a crear el código completo con las rutas faltantes para los Reportes de IVA y Rentabilidad. Te voy a proporcionar todo el código adicional que necesitas agregar a tu app.py.

Código adicional para app.py - Reportes IVA y Rentabilidad
Código 
espera, crear el codigo completo en 4 partes, porque sini se te vaa  cortar

¡Perfecto! Voy a crear el código completo en 4 partes para que no se corte. Empecemos:

PARTE 1/4 - Reporte de IVA (Rutas principales)
Código 
espera, pero crear todoelcodigo completo, totalmente corregido 4 cuatro partes

¡Perfecto! Voy a crear el código COMPLETO y TOTALMENTE CORREGIDO en 4 partes. Aquí vamos:

PARTE 1/4 - app.py COMPLETO con correcciones
Código 
que hicistes, lo enviaste incompleto iniciar de nuevo con las 4 partes de codigo

¡Tienes razón! Disculpa. Voy a crear las 4 partes COMPLETAS del código desde el inicio. Aquí vamos:

PARTE 1/4 - Código app.py (Inicio hasta Usuarios)
Código 
que estas hacien hombre, estas cortando todo, envia cada parte por aparete en diferente archivo y cuando envies laprimera parete de las 4 partes yo de indico cuando en viacada parte

¡Tienes razón! Disculpa. Voy a enviar SOLO LA PARTE 1 completa y espero tu confirmación para continuar con la PARTE 2.

PARTE 1 DE 4 - Configuración y Funciones Base
Código 
✅ PARTE 1 DE 4 ENVIADA

Esta parte incluye:

✅ Importaciones y configuración de Flask
✅ Conexión a MySQL
✅ Funciones de productos
✅ Funciones de ventas
✅ Funciones de usuarios
✅ Funciones de logs
✅ Decoradores de autenticación
👉 Confirma que recibiste esta PARTE 1 correctamente y te envío la PARTE 2



¿Cómo puedo ayudarle hoy?






# ===============================================================================
# SISTEMA DE INVENTARIO DE REPUESTOS PARA MOTOS - MOTORREPUESTOS H&D
# PARTE 1 DE 4 - CONFIGURACIÓN INICIAL Y FUNCIONES BASE
# ===============================================================================

from flask import Flask, request, render_template, redirect, url_for, flash, send_file, session
import json
import os
from datetime import datetime
from functools import wraps
from werkzeug.security import generate_password_hash, check_password_hash
import pymysql

app = Flask(__name__)
app.secret_key = "dev_secret_key_change_in_production"


# ---------------------------------------------------------------------------------
# CONFIGURACIÓN Y CONEXIÓN A MYSQL
# ---------------------------------------------------------------------------------
def get_db_connection():
    """Crea y retorna una conexión a MySQL"""
    return pymysql.connect(
        host='127.0.0.1',
        user='root',
        password='',  # XAMPP no tiene contraseña por defecto
        database='inventario_repuestos',
        cursorclass=pymysql.cursors.DictCursor
    )


def ejecutar_query(query, params=None, commit=False, fetch_one=False, fetch_all=False):
    """
    Función auxiliar para ejecutar consultas SQL de forma segura.
    
    Args:
        query: Consulta SQL con placeholders %s
        params: Tupla con los parámetros
        commit: True si es INSERT/UPDATE/DELETE
        fetch_one: True para obtener un solo registro
        fetch_all: True para obtener todos los registros
    
    Returns:
        - Si fetch_one: Un diccionario o None
        - Si fetch_all: Lista de diccionarios
        - Si commit: ID del último registro insertado (lastrowid)
    """
    connection = None
    cursor = None
    try:
        connection = get_db_connection()
        cursor = connection.cursor()
        cursor.execute(query, params or ())
        
        if commit:
            connection.commit()
            last_id = cursor.lastrowid
            return last_id
        
        if fetch_one:
            result = cursor.fetchone()
            return result
        
        if fetch_all:
            results = cursor.fetchall()
            return results
        
        return None
        
    except Exception as e:
        print(f"❌ Error en la base de datos: {e}")
        if commit and connection:
            connection.rollback()
        return None
    finally:
        if cursor:
            cursor.close()
        if connection:
            connection.close()


# ---------------------------------------------------------------------------------
# CONSTANTES
# ---------------------------------------------------------------------------------
STOCK_MINIMO = 5

# Roles disponibles
ROLES = {
    'admin': 'Administrador',
    'vendedor': 'Vendedor',
    'auditor': 'Auditor'
}


# ---------------------------------------------------------------------------------
# FUNCIONES DE CARGA Y GUARDADO (MYSQL) - PRODUCTOS
# ---------------------------------------------------------------------------------
def cargar_productos():
    """Carga todos los productos desde MySQL"""
    query = """
        SELECT id, nombre, categoria, marca, stock, precio_unitario, 
               descripcion, valor_total, fecha_creacion, fecha_actualizacion
        FROM productos
        ORDER BY id
    """
    productos = ejecutar_query(query, fetch_all=True)
    return productos if productos else []


def obtener_producto_por_id(producto_id):
    """Obtiene un producto específico por su ID"""
    query = """
        SELECT id, nombre, categoria, marca, stock, precio_unitario, 
               descripcion, valor_total, fecha_creacion, fecha_actualizacion
        FROM productos
        WHERE id = %s
    """
    return ejecutar_query(query, (producto_id,), fetch_one=True)


def actualizar_producto(producto_id, nombre, categoria, marca, stock, precio_unitario, descripcion):
    """Actualiza un producto existente en MySQL"""
    valor_total = round(stock * precio_unitario, 3)
    
    query = """
        UPDATE productos 
        SET nombre = %s, categoria = %s, marca = %s, stock = %s, 
            precio_unitario = %s, descripcion = %s, valor_total = %s,
            fecha_actualizacion = NOW()
        WHERE id = %s
    """
    params = (nombre, categoria, marca, stock, precio_unitario, descripcion, valor_total, producto_id)
    return ejecutar_query(query, params, commit=True)


def eliminar_producto_db(producto_id):
    """Elimina un producto de MySQL"""
    query = "DELETE FROM productos WHERE id = %s"
    return ejecutar_query(query, (producto_id,), commit=True)


def actualizar_stock_producto(producto_id, nuevo_stock):
    """Actualiza solo el stock de un producto"""
    producto = obtener_producto_por_id(producto_id)
    if not producto:
        return False
    
    valor_total = round(nuevo_stock * producto['precio_unitario'], 3)
    
    query = """
        UPDATE productos 
        SET stock = %s, valor_total = %s, fecha_actualizacion = NOW()
        WHERE id = %s
    """
    return ejecutar_query(query, (nuevo_stock, valor_total, producto_id), commit=True)


# ---------------------------------------------------------------------------------
# FUNCIONES DE VENTAS
# ---------------------------------------------------------------------------------
def cargar_ventas():
    """Carga todas las ventas desde MySQL"""
    query = """
        SELECT id, fecha, hora, producto_id, producto_nombre, categoria,
               cantidad, precio_unitario, total, usuario_id, usuario_nombre,
               fecha_registro
        FROM ventas
        ORDER BY fecha DESC, hora DESC
    """
    ventas = ejecutar_query(query, fetch_all=True)
    return ventas if ventas else []


def guardar_venta(venta):
    """Guarda una nueva venta en MySQL"""
    query = """
        INSERT INTO ventas (fecha, hora, producto_id, producto_nombre, categoria,
                           cantidad, precio_unitario, total, usuario_id, usuario_nombre)
        VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
    """
    params = (
        venta['fecha'],
        venta['hora'],
        venta['producto_id'],
        venta['producto_nombre'],
        venta['categoria'],
        venta['cantidad'],
        venta['precio_unitario'],
        venta['total'],
        venta.get('usuario_id'),
        venta.get('usuario_nombre')
    )
    return ejecutar_query(query, params, commit=True)


# ---------------------------------------------------------------------------------
# FUNCIONES DE USUARIOS Y AUTENTICACIÓN
# ---------------------------------------------------------------------------------
def cargar_usuarios():
    """Carga todos los usuarios desde MySQL"""
    query = """
        SELECT id, username, password, nombre_completo, rol, activo, 
               fecha_creacion, fecha_actualizacion
        FROM usuarios
        ORDER BY id
    """
    usuarios = ejecutar_query(query, fetch_all=True)
    
    # Si no hay usuarios, crear admin por defecto
    if not usuarios:
        query_insert = """
            INSERT INTO usuarios (username, password, nombre_completo, rol, activo)
            VALUES (%s, %s, %s, %s, %s)
        """
        ejecutar_query(
            query_insert,
            ('admin', generate_password_hash('admin123'), 'Administrador', 'admin', True),
            commit=True
        )
        return cargar_usuarios()
    
    return usuarios


def crear_usuario(username, password, nombre_completo, rol):
    """Crea un nuevo usuario en MySQL"""
    query = """
        INSERT INTO usuarios (username, password, nombre_completo, rol, activo)
        VALUES (%s, %s, %s, %s, %s)
    """
    password_hash = generate_password_hash(password)
    params = (username, password_hash, nombre_completo, rol, True)
    return ejecutar_query(query, params, commit=True)


def actualizar_usuario_estado(usuario_id, activo):
    """Activa o desactiva un usuario"""
    query = """
        UPDATE usuarios 
        SET activo = %s, fecha_actualizacion = NOW()
        WHERE id = %s
    """
    return ejecutar_query(query, (activo, usuario_id), commit=True)


def eliminar_usuario_db(usuario_id):
    """Elimina un usuario de MySQL"""
    query = "DELETE FROM usuarios WHERE id = %s"
    return ejecutar_query(query, (usuario_id,), commit=True)


def actualizar_password_usuario(usuario_id, nueva_password):
    """Actualiza la contraseña de un usuario"""
    query = """
        UPDATE usuarios 
        SET password = %s, fecha_actualizacion = NOW()
        WHERE id = %s
    """
    password_hash = generate_password_hash(nueva_password)
    return ejecutar_query(query, (password_hash, usuario_id), commit=True)


def obtener_usuario_por_id(usuario_id):
    """Obtiene un usuario específico por su ID"""
    query = """
        SELECT id, username, password, nombre_completo, rol, activo, 
               fecha_creacion, fecha_actualizacion
        FROM usuarios
        WHERE id = %s
    """
    return ejecutar_query(query, (usuario_id,), fetch_one=True)


def registrar_log(accion, detalle=""):
    """Registra una acción en los logs de MySQL"""
    query = """
        INSERT INTO logs (fecha, hora, usuario, accion, detalle)
        VALUES (%s, %s, %s, %s, %s)
    """
    params = (
        datetime.now().strftime('%Y-%m-%d'),
        datetime.now().strftime('%H:%M:%S'),
        session.get('username', 'Sistema'),
        accion,
        detalle
    )
    ejecutar_query(query, params, commit=True)


def cargar_logs():
    """Carga todos los logs desde MySQL"""
    query = """
    SELECT id, fecha, hora, usuario, accion, detalle, fecha_registro
    FROM logs
    ORDER BY fecha_registro ASC
    """
    logs = ejecutar_query(query, fetch_all=True)
    return logs if logs else []


# ---------------------------------------------------------------------------------
# DECORADORES DE AUTENTICACIÓN
# ---------------------------------------------------------------------------------
def login_required(f):
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if 'user_id' not in session:
            flash('Debes iniciar sesión para acceder a esta página.', 'warning')
            return redirect(url_for('login'))
        return f(*args, **kwargs)
    return decorated_function


def role_required(*roles):
    def decorator(f):
        @wraps(f)
        def decorated_function(*args, **kwargs):
            if 'user_id' not in session:
                flash('Debes iniciar sesión.', 'warning')
                return redirect(url_for('login'))
            
            if session.get('rol') not in roles:
                flash('No tienes permisos para acceder a esta página.', 'danger')
                return redirect(url_for('inicio'))
            
            return f(*args, **kwargs)
        return decorated_function
    return decorator